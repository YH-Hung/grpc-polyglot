syntax = "proto3";

package secrets;

message AuditEntry {
  string actor = 1;
  bytes detail = 2;
}

message AuditAck {
  bool ok = 1;
}

service AuditService {
  rpc Record(AuditEntry) returns (AuditAck) {}
}
